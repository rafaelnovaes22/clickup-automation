Antes de explorar o código para localizar um comportamento, leia HARNESS_HANDBOOK.md (mapa comportamento→código com âncoras).

Leia `.claude/CONSTITUTION.md` antes de agir. Este é um consumidor Foundry do tipo `automation`, sem inferência LLM no runtime. Contrato: `docs/foundry/project.json`. Decisão: `docs/adr/20260904-consumer-governance.md`. Gates locais: `npm test`, `npm run validate`, `bash scripts/foundry-doctor.sh --consumer`.
